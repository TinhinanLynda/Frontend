{
  "name": "desktop-test",
  "version": "1.0.0",
  "description": "my",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/TinhinanLynda/Frontend.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-updater": "^4.3.9"
  },
  "devDependencies": {
    "electron": "^15.3.0",
    "electron-builder": "^22.13.1"
  }
}